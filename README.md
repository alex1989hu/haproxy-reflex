# haproxy-reflex

A minimal, distroless container image for [`reflex`](https://github.com/cespare/reflex), a small utility that watches files and runs a command when they change. Built for use as a config-reload sidecar for HAProxy in Kubernetes.
